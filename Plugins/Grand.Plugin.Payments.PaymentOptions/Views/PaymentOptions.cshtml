@using Grand.Plugin.Payments.PaymentOptions.Models;
@using System.Web;

@model PaymentOptionModel;
@{
    Layout = "_ColumnsOne";
}
<article class="card">
    <div class="card-body p-5">
        <ul class="nav bg-light nav-pills rounded nav-fill mb-3" role="tablist">
            @*<li class="nav-item" style="@(Model.IsActiveWithOutThreeDSecure == true ? "display:block;" : "display:none;")">
                    <a class="nav-link active" data-toggle="pill" href="#nav-tab-card">
                        <i class="fa fa-credit-card"></i> Kredi Kartı
                    </a>
                </li>*@
            @if (!Model.IsRoleGenelMudurluk)
            {
                <li class="nav-item" style="@(Model.IsActiveThreeDSecure == true ? "display:block;" : "display:none;")">
                    <a class="nav-link active" data-toggle="pill" href="#nav-tab-with-3d">
                        <i class="fa fa-credit-card"></i> Kredi Kartı (3D Secure)
                    </a>
                </li>
            }
            @*<li class="nav-item" style="@(Model.IsActiveHavale == true ? "display:block;" : "display:none;")">
                <a class="nav-link" data-toggle="pill" href="#nav-tab-with-havale">
                    <i class="fa fa-money"></i> Havale
                </a>
            </li>*@
            @*<li class="nav-item">
                    <a class="nav-link" data-toggle="pill" href="#nav-tab-paypal">
                        <i class="fab fa-paypal"></i>  Paypal
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" data-toggle="pill" href="#nav-tab-bank">
                        <i class="fa fa-university"></i>  Bank Transfer
                    </a>
                </li>*@
        </ul>
        <div class="tab-content">
            @if (!Model.IsRoleGenelMudurluk)
            {
                <div id="nav-tab-with-3d" class="tab-pane fade show active">
                    <partial name="3D_Secure.cshtml" />
                </div>
                @*<div id="nav-tab-with-havale" class="tab-pane fade">
                    <partial name="Havale.cshtml" />
                </div>*@
            }
            else
            {
                @*<div id="nav-tab-with-havale" class="tab-pane fade show active">
                    <partial name="Havale.cshtml" />
                </div>*@  
            }
            
            @*@if (Model.IsActiveWithOutThreeDSecure)
        {
            <div id="nav-tab-card" class="tab-pane fade show active">
                <partial name="Without_3D_Secure.cshtml" />
            </div>
        }
        @if (Model.IsActiveThreeDSecure)
        {
            <div id="nav-tab-with-3d" class="tab-pane fade show">
                <partial name="3D_Secure.cshtml" />
            </div>
        }
        @if (Model.IsActiveHavale)
        {
            <div id="nav-tab-with-havale" class="tab-pane fade show active">
                <partial name="Havale.cshtml" />
            </div>
        }*@
        </div>
    </div> <!-- card-body.// -->
</article> <!-- card.// -->
<script>
    function validate(ev) {
        if (!ev) {
            ev = window.event;
        }
        if (!ev.ctrlKey && ev.key.length === 1 && (isNaN(+ev.key) || ev.key === " ")) {
            return ev.preventDefault();
        }
    }
</script>